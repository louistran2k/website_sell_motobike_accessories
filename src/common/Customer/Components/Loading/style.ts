import { makeStyles } from '@mui/styles';
export const useStyles = makeStyles({
  loading: {
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  loading__gif: {
    width: '300px',
    display: 'block',
  },
});
